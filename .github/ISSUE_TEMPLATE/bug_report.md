# bug_report.md

---

## name: Bug report about: Report a reproducible bug in Manifest-InX. title: "bug: " labels: [bug]

## Version (required)

Provide one:

- `manifestinx --version` (if available)
- `pip show manifestinx` → `Version`
- Manifest version: `src/manifestinx/data/manifestinx_manifest_v1.json` → `version`
- API health: `GET /health` → `version`

Version:

## Exact command run (required)

Paste the exact command(s) executed:

```text
<command>
```

## Expected vs actual (required)

Expected:

Actual:

## Golden regression changed? (required)

- Result: (pass / fail / skipped)
- If changed: describe which case(s) and whether a golden version bump is intended

## Environment (required)

- OS:
- Python:
- Install method: (editable `pip install -e .` / wheel / other)
- Install source: (from tag vX.Y.Z / main / commit SHA)

## Repro steps (optional)

1.
2.
3.

## Logs / output (optional)

```text
<paste logs>
```